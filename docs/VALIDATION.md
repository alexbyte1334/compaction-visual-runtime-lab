# 验证记录

验证日期：2026-09-10。环境：本地 macOS / Python 3.13.12；直接依赖版本固定于 requirements.txt。这里是本次实测快照，数字不作为跨机器不变的 golden 值。

## 实际结果

`python -m pytest -q`：**22 passed**。涵盖真实修复、五类故障拒绝、逐阶段惰性执行、阈值以下跳过、保护项预算不足、非法 Provider 输出、工具配对、工具文本信任层级、基线公平性、网络连接禁止、输入窗口超限、源码漂移和 Streamlit 页面完整交互。

`python -m lab.cli compare --output runs/comparison.json`：

| 策略 | 输入 Token | 候选 Token | 减少比例 | 恢复结果 |
|---|---:|---:|---:|---|
| full | 3984 | 3984 | 0% | passed |
| truncate | 3984 | 165 | 95.86% | blocked，缺失目标/约束 |
| structured | 3984 | 1194 | 70.03% | passed |

结构化组全部校验通过并提交候选；原始用例 2 failed / 81 passed，修复后 83 passed。完整组是超过压缩目标的参考组，不能因为“能修复”而称其满足压缩预算。截断组没有执行破坏性替代方案，实际结果是拒绝修改。

本地浏览器已检查初始界面、真实案例初始化、压缩后的 COMMITTED 状态；自动化 UI 测试额外覆盖恢复测试、三组对照和拒绝后按钮禁用。

## 如何复核

```bash
python -m pytest -q
python -m lab.cli compare --output runs/comparison.json
```

每份对照报告保留同一 snapshot_sha256，记录完整输入/候选/已提交上下文、逐阶段事件、验证字段、恢复补丁与测试标准输出。`runs/` 不自动提交，避免以后混入工作日志。

## 没有验证或不能推断的事项

- 尚无 API 调用、真实模型质量、费用或模型延迟数据。
- 单步时间包含用户等待；不是计算耗时基准。
- 没有一般语义等价证明，也没有验证无限多轮压缩。
- 测试覆盖的是受控合成案例；80 个诊断参数用例不是 80 个真实业务场景。
- 公司电脑的网络、GitHub 登录、依赖安装权限需要在公司环境验证。
- CI 配置包含 Linux / Python 3.11 与 3.13；实时结果以仓库 Actions 为准。
