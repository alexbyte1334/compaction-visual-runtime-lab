# Compaction Visual Runtime Lab

版本：1.2 · 状态：当前唯一需求基线 · 更新：2026-09-10

本文件定义项目目标、范围、行为与验收标准。README 负责启动说明，docs/ARCHITECTURE.md 解释实现，docs/HANDOFF.md 记录后续工作；其他材料不另行定义需求。实现完成情况以代码、测试与验证记录为准。

## 1. 项目目标

构建一个小型、本地可运行的上下文压缩实验台，用实际故障任务展示：Agent Runtime 如何在 Token 预算下保留必要信息，验证压缩候选，安全交接给下一步执行。

核心实验问题：**同一个故障任务在预算受限时，哪种上下文处理还能让任务正确继续？压缩失败时，Runtime 如何保护原状态？**

组会分享需要同时提供可执行案例、可检查的机制、相同条件下的对照与失败证据。仅有摘要、压缩率或流程动画不能视为成功。

## 2. 用户与场景

主要用户是开发者和技术分享参与者。讲者可以单步执行、检查输入输出、改变预算或注入故障；观众能够从源码、状态与测试结果理解机制。

固定业务案例：refresh token endpoint 错用 access 类型验证，导致 refresh token 被拒绝、access token 被错误接受。实验使用真实 PyJWT 与 pytest，保持签名校验逻辑和 JWT 库不变。

案例是独立的合成学习实验，不代表工作中的生产事故。讲者应亲自复跑、修改参数并记录观察。如果使用工作案例，应另行取得许可并检查内容，不能将内部代码、日志或凭据直接加入仓库。

## 3. 交付阶段

| 阶段 | 交付内容 | 完成条件 |
|---|---|---|
| v0.1 离线初步框架 | 真实故障采集、规则压缩器、可观测 Runtime、脚本恢复 Worker、三组对照、故障注入、技术控制台、Trace、测试与 CI | 新机器可运行；正常实验和拒绝路径有真实证据 |
| v0.2 模型实验 | 用户后续接入模型 Provider 和模型 Worker，真实录制/回放、用量与耗时、多案例评测 | 相同任务条件下验证模型输出和实际任务结果 |
| 后续扩展 | 多轮压缩、跨代证据索引、跨进程 checkpoint 与副作用对账 | 专项故障与恢复测试通过后再声明支持 |

当前开发范围为 v0.1。无需密钥或模型 API；依赖与 tokenizer 词表首次准备需要网络，之后实验离线运行。v0.2 与后续扩展不得被标成已完成。

## 4. 核心运行流程

```text
执行合成故障 → 采集源码和测试结果
  → snapshot
  → trigger
  → policy_request
  → selection
  → reconstruction
  → validation
  → commit / abort
  → continue：校验任务交接 → 应用局部修复 → 运行回归测试
```

这七个 Runtime 阶段必须可以逐步执行。一次点击只能推进一个实际阶段；连续运行复用同一执行器。UI 只能展示已发生的事件，不能提前填入结果。

### 4.1 故障输入与快照

- 在临时目录复制可信合成夹具，真实运行测试，采集源码、工具调用和工具结果。
- 目标、明确约束、未决问题、任务状态和下一步单独建模，标注来源类型。
- ContextItem 至少包含 id、kind、content、call_id；同一次工具调用的 call/result 应完整配对。
- 冻结输入副本并记录哈希；记录逐项加入后的累计 Token，原始证据可供检查。

### 4.2 触发与预算

- 对完整规范 JSON 信封计数，包括 system、工具定义、items 和 compacted_state。
- 使用 cl100k_base 的真实 tokenizer；不得将字符数或演示常量冒充 Token。
- 默认实验参数：window=20000、trigger=3500、target=1800、输出预留=1200、下一工具结果预留=600。
- 保证 `0 < target < trigger <= window - reserves`；阈值以下保留原上下文。
- 输入已超过安全窗口或存在未完成工具调用时终止本次压缩，不调用压缩器。
- 计算保护项下限与历史可用额度，重建后重新对整个信封计数。超预算时拒绝提交。
- 这些参数和计数只用于本地实验，不等于某个模型的真实窗口限制或 API 计费量。

### 4.3 Policy 与 Provider

- 逐字保留 goal、constraint、open_issue、state、decision，以及最近两项；按 ID 去重。
- Provider 接收明确 policy、快照、预算和输出合同，返回逐项 KEEP / COMPRESS / DROP、简短理由与引文。
- 所有源 ID 必须且只能被处理一次；非法 schema、重复 ID、未知 action 应拒绝。
- v0.1 的 RuleProvider 必须明确标注为规则实现，不显示虚构的相关性评分或模型思维过程。
- Provider 只产生候选提议，不拥有状态提交或文件修改权限。
- v0.1 按受控 pytest 格式抽取原文，不承诺通用摘要或自适应多轮压缩。

### 4.4 重建与校验

- 候选包含保留条目、带出处的压缩证据与派生的工作状态。
- 每条引文包含 source_id、源哈希和原文 quote；quote 必须能在相应原文中找到。
- Runtime 继承来源的 kind 和 call_id，工具内容不得被提升为用户约束或系统指令。
- 独立校验目标、约束、未决问题、任务状态、最近信息、工具配对、证据、预算和状态一致性。
- 字符串存在与引文匹配只能证明对应的结构条件，不能宣称一般语义等价。
- 区分 before、candidate、current；只有所有校验通过才更新 current，否则原上下文保持不变。
- 界面和报告分别显示候选 Token 和已提交 Token，失败候选不能被包装成已完成的压缩。

### 4.5 恢复执行

- v0.1 使用明确标注的脚本 Worker，不冒充模型继续推理。
- Worker 只接收交付的上下文与当前临时文件，不接收策略名称、原始快照或校验结果捷径。
- 恢复前核对任务合同、失败证据与源码一致性；缺失信息或文件漂移时停止修改。
- 应用范围受控的一行修复，真实运行回归测试，记录 diff、文件哈希、退出码与输出。
- Runtime 拒绝候选时禁用恢复；已经执行过恢复时禁止重复点击同一修复。

## 5. 对照与故障实验

三组必须使用同一输入快照、相同 Worker 与独立的故障副本：

| 策略 | 处理方式 | 评价方式 |
|---|---|---|
| full | 保留完整上下文，不调用压缩器 | 完整输入参考，单独记录是否超过压缩目标 |
| truncate | 按最老优先删除完整条目/工具对至目标内 | 检查保留合同与实际恢复结果，不预设模型行为 |
| structured | 按保护策略提议、重建、验证 | 同时满足预算、保留约束和真实任务结果 |

truncate 与 structured 共享压缩目标。full 不应因能完成任务就被称为满足压缩预算。报告包含输入/候选/已提交 Token、减少比例、约束检查、任务结果和同一快照哈希。

必须支持五类故障：丢失约束、伪造引文、候选超预算、未完成工具调用、Provider 超时。每类故障都应展示拒绝原因、原状态保留和未执行修复。

## 6. Web 技术展示规范

界面定位为类似终端 CLI 的技术控制台，以数据与运行证据为核心。

- 使用系统等宽字体、单色深底、克制的字号和细分隔线；保留清晰的键盘焦点和禁用状态。
- 标题仅使用项目名称；不使用营销式疑问句、图标装饰、渐变、插图、指标卡墙或模拟打字动画。
- 首屏包含模式、故障/目标配置、当前运行状态、before/candidate/active Token、恢复结果与真实控制按钮。
- 单步执行以紧凑逐行日志显示阶段、关键字段及明确的 PASS / FAIL；文字本身必须足以区分结果。
- 信息取舍和三组对照使用数值对齐的表格；理由、原始请求、证据和完整 JSON 放在检查视图。
- 正常运行、未运行、运行中、拒绝与恢复完成状态都要准确；配置变更尚未应用时明确提示。
- 主要交互为创建/重置、下一阶段、运行剩余阶段、恢复测试、三组对照与 JSON 导出。
- 长表只在表格区域内横向滚动；窄屏控件能换行，主体不得出现横向溢出或文字遮挡。
- 增长数据保留为可检查的表格。累计墙钟时间包含手动等待，不作为计算延迟展示。
- Web 与 CLI 共用实验逻辑，UI 不维护第二套成功判定。

## 7. 记录与工程边界

Trace 至少包含 schema 版本、离线模式、Provider、策略、故障、预算、快照哈希、输入/候选/当前状态、事件、验证和恢复证据。导出 JSON 不等于已实现跨进程 checkpoint。

依赖版本固定，提供 macOS/Linux 与 Windows 启动方式。测试只对可信合成案例执行；不接受任意上传脚本或公司日志。虚拟环境、词表缓存、运行产物和凭据不提交到 GitHub。

不引入多 Agent、MCP、RAG、向量库、数据库、用户系统或分布式 Runtime。原生厂商 compaction 是独立接口能力，本项目明文引文和规则不能被描述为其不可观测内部实现。

## 8. 验收标准

### 离线框架

1. 按 README 从仓库安装与启动，CLI 与 Web 都能运行，不调用模型 API。
2. 真实复现两处认证行为错误；当前夹具输出为 2 failed / 81 passed，修复后 83 passed。
3. 七阶段单步的输入输出可以检查，UI 不提前执行后续阶段。
4. 正常压缩满足预算与保留合同；只有校验通过后才允许恢复执行。
5. 五类故障拒绝提交；原状态不变、不运行修复。
6. 对照组使用同一快照且无需压缩器，报告展示实际恢复结果。
7. 证明有 Token 减少时同时说明保留条件和任务结果，不使用固定数字作为验收捷径。
8. 技术控制台正常路径、拒绝路径、对照与导出可用；桌面和窄屏数据可读。
9. 自动化测试、远端 CI 与实际验证记录可复核。

### 分享准备与后续模型版本

讲者独立复跑并修改至少一个约束、预算或故障点，能够从阶段日志追到源码/引文，解释候选为何被接受或拒绝。合成案例应如实说明。

模型版本接入后，另行记录实际请求响应、模型与配置、用量、耗时，以及多个案例至少三次重复的结果。再加入真实响应回放、多轮压缩和任务完成率验证；不得以离线规则测试代替模型质量评测。
